---
name: assets-prefab-close
description: Close the current Unity Prefab Stage through Pipeline.
---

# Prefabs / Close Stage

```powershell
unity command close_prefab_stage --save true
```

To discard unsaved stage edits, pass both `--save false` and `--confirm true`.
