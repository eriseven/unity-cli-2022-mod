---
name: profiler-list-modules
description: List supported Pipeline Profiler-module preferences.
---

# Profiler / List Modules

```powershell
unity command profiler_list_modules
```

This returns the supported inspection categories and the current Pipeline-session preference for each; it does not inspect Unity's private Profiler-window layout.
