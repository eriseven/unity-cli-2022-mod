---
name: profiler-get-memory-stats
description: Read current Unity allocation and managed-heap counters through Pipeline.
---

# Profiler / Memory Snapshot

```powershell
unity command profiler_get_memory_stats
```

Values are bytes from Unity's built-in `Profiler` API.
