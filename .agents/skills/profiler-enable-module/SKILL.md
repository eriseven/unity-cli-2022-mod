---
name: profiler-enable-module
description: Set Pipeline's Profiler module-inspection preference for the current Editor session.
---

# Profiler / Enable Module

```powershell
unity command profiler_list_modules
unity command profiler_enable_module --module "Memory" --enabled true
```

The setting records the Pipeline session's inspection intent. Unity's internal Profiler-window layout remains under Unity's UI control.
